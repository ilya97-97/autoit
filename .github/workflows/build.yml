name: run-tests
on: [push]
jobs:
  test:
    name: Run hello world
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run script
        run: bash hello-world.sh